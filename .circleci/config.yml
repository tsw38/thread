version: 2.1


jobs:
  build:
    docker:
      - image: 'circleci/node:12.13.0'
    steps:
      - checkout
      - run:
          name: Install Node Modules
          command: yarn install && yarn test
